export enum VideoType {
	CAMERA = 'CAMERA',
	SCREEN = 'SCREEN',
	CUSTOM = 'CUSTOM'
}

export enum ScreenType {
	WINDOW = 'window',
	SCREEN = 'screen'
}

export enum VideoSizeIcon {
	BIG = 'fullscreen',
	NORMAL = 'fullscreen_exit'
}
