# openvidu-embedded-java-client

OpenVidu Embedded API

- API version: v1

- Generator version: 7.11.0

The OpenVidu Embedded API allows seamless integration of interactive video rooms into external applications via an iframe. This API provides endpoints to manage participants, generate secure access URLs, and configure session settings for an embedded experience.


  For more information, please visit [https://openvidu.io/support/](https://openvidu.io/support/)

*Automatically generated by the [OpenAPI Generator](https://openapi-generator.tech)*

## Requirements

Building the API client library requires:

1. Java 1.8+
2. Maven/Gradle

## Installation

To install the API client library to your local Maven repository, simply execute:

```shell
mvn clean install
```

To deploy it to a remote Maven repository instead, configure the settings of the repository and execute:

```shell
mvn clean deploy
```

Refer to the [OSSRH Guide](http://central.sonatype.org/pages/ossrh-guide.html) for more information.

### Maven users

Add this dependency to your project's POM:

```xml
<dependency>
  <groupId>io.openvidu</groupId>
  <artifactId>openvidu-embedded-java-client</artifactId>
  <version>1.0.0</version>
  <scope>compile</scope>
</dependency>
```

### Gradle users

Add this dependency to your project's build file:

```groovy
compile "io.openvidu:openvidu-embedded-java-client:1.0.0"
```

### Others

At first generate the JAR by executing:

```shell
mvn clean package
```

Then manually install the following JARs:

- `target/openvidu-embedded-java-client-1.0.0.jar`
- `target/lib/*.jar`

## Getting Started

Please follow the [installation](#installation) instruction and execute the following Java code:

```java

import io.openvidu.embedded.client.*;
import io.openvidu.embedded.client.auth.*;
import io.openvidu.embedded.model.*;
import io.openvidu.embedded.api.OpenViduEmbeddedClient;

public class OpenViduEmbeddedClientExample {

    public static void main(String[] args) {
        ApiClient defaultClient = Configuration.getDefaultApiClient();
        defaultClient.setBasePath("http://localhost:6080/embedded/api/v1");
        
        OpenViduEmbeddedClient apiInstance = new OpenViduEmbeddedClient(defaultClient);
        EmbeddedTokenOptions embeddedTokenOptions = new EmbeddedTokenOptions(); // EmbeddedTokenOptions | 
        try {
            AddParticipant200Response result = apiInstance.addParticipant(embeddedTokenOptions);
            System.out.println(result);
        } catch (ApiException e) {
            System.err.println("Exception when calling OpenViduEmbeddedClient#addParticipant");
            System.err.println("Status code: " + e.getCode());
            System.err.println("Reason: " + e.getResponseBody());
            System.err.println("Response headers: " + e.getResponseHeaders());
            e.printStackTrace();
        }
    }
}

```

## Documentation for API Endpoints

All URIs are relative to *http://localhost:6080/embedded/api/v1*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*OpenViduEmbeddedClient* | [**addParticipant**](docs/OpenViduEmbeddedClient.md#addParticipant) | **POST** /participant | Generate a participant URL for an embedded room


## Documentation for Models

 - [AddParticipant200Response](docs/AddParticipant200Response.md)
 - [EmbeddedParticipantPermissions](docs/EmbeddedParticipantPermissions.md)
 - [EmbeddedTokenOptions](docs/EmbeddedTokenOptions.md)
 - [Error](docs/Error.md)


<a id="documentation-for-authorization"></a>
## Documentation for Authorization

Endpoints do not require authorization.


## Recommendation

It's recommended to create an instance of `ApiClient` per thread in a multithreaded environment to avoid any potential issues.

## Author

commercial@openvidu.io

